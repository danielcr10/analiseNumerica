#include <stdio.h>
#include <math.h>

double f(x){
  return cos(x) - 2*sin(x);
}

double fLinha(x){
  return -sin(x) - 2*cos(x);
}

int main(){
  //Avalia h ótimo em f(x) e em fLinha(x) e compara
  
  
  return 0;
}
